//! Restricted Text strings and I/O streams
//!
//! Restricted Text is more a collection of ideas than an actual format. The
//! first step is to combine them into a coherent format which could be
//! implemented.

#![deny(missing_docs)]
#![cfg_attr(can_vector, feature(can_vector))]
#![cfg_attr(write_all_vectored, feature(write_all_vectored))]
#![cfg_attr(pattern, feature(pattern))]
